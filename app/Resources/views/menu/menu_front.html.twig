<nav class="navbar navbar-default">
    <div class="container">
          <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#menu-front" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="{{path('ticket_index')}}">SamTicket</a>
    </div>
         <div class="collapse navbar-collapse" id="menu-front">
      <ul class="nav navbar-nav">
        <li class="active"><a href="{{path('ticket_index')}}">Create Ticket<span class="sr-only">(current)</span></a></li>
        <li><a href="#">Check Status</a></li>
        {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
        <li><a href="{{path('staff_dashboard')}}">Dashboard</a></li>
        {% endif %}
      </ul>
            <ul class="nav navbar-nav navbar-right">
                {% if app.user and is_granted('IS_AUTHENTICATED_FULLY') %}
                    <li> <a href="{{path('login_logout')}}">Log Out</a></li>
        {% else %}
                    <li><a href="{{path('login_route')}}">Log In</a></li>
{% endif %}
      </ul>
    </div>
    </div>
</nav>
